<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <title>My Userspace</title>
</head>
<body class="bg-zinc-900 ">
    <div class ="w-full h-full p-10 text-white">
        <div class=" nav mb-5">
            <a class="text-blue-500" href="/">Home</a>
        </div>
        <h1 class="tracking-tighter text-3xl mb-3 ">All Users</h1>

        <div class = "users flex gap-3 flex-wrap">

        <%if (users.length > 0) { 
          users.forEach((val)=>{ %>
        
            <div class="user w-72 p-4 bg-zinc-800 rounded-lg  ">
                <div class="w-full h-52 rounded-lg bg-zinc-700 overflow-hidden">
                    <img class="w-full h-full object-cover" src="<%=val.image%>" alt="Photo">
                </div>
                 <h3 class="text-xl tracking-tighter mt-3"><%=val.name%></h3>
                 <h5 class="email text-zinc-500"><%=val.email%></h5>
                 <div class="flex gap-3 mt-10">
                    <a class="text-zinc-200" href="/edit/<%= val._id%>">Edit User</a>
                    <a class="text-red-500" href="/delete/<%= val._id%>">Delete User</a>
                 </div>
            </div>
            
            <% }) %>
        <% } else { %>
            <h3 class = "text-zinc-500 ">No Users yet</h3>
            <% } %>
        </div>
    
</body>
</html>